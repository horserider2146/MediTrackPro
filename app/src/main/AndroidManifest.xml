<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Notifications -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" /> <!-- Camera -->
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" /> <!-- Internet -->
    <uses-permission android:name="android.permission.INTERNET" /> <!-- Wearable -->
    <uses-permission android:name="android.permission.BODY_SENSORS" />
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC"/>
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MediTrackPro">
        <activity
            android:name=".AddMedicineActivity"
            android:exported="false" />
        <!-- LoginActivity launches first -->
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- MainActivity — no longer the launcher -->
        <activity
            android:name=".MainActivity"
            android:exported="false" /> <!-- SignUpActivity -->
        <activity
            android:name=".SignUpActivity"
            android:exported="false" />
        <activity android:name=".NotificationSettingsActivity" android:exported="false"/>
        <activity android:name=".PrivacySettingsActivity" android:exported="false"/>
        <activity android:name=".EmergencyContactsActivity" android:exported="false"/>
        <activity android:name=".AppAppearanceActivity" android:exported="false"/>
        <activity android:name=".AddHealthRecordActivity" android:exported="false"/>
        <activity android:name=".EditHealthInfoActivity" android:exported="false"/>
        <activity android:name=".CaregiverSettingsActivity" android:exported="false"/>
    </application>

</manifest>